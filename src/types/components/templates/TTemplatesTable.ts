export type TTemplatesTable = {
    isOverFlow: boolean;
    isCompact: boolean;
    width: string;
    hover: boolean;
    heads: Array<object>;
    foots: Array<object>;
    bodies: Array<object> | null;
};
